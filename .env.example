# Copy this file to .env and fill in real values
# .env is git-ignored — never commit actual credentials

# HuggingFace API token (required for Qwen2.5-7B-Instruct)
HF_TOKEN=hf_your_token_here

# Chainlit JWT secret — generate with: chainlit create-secret
CHAINLIT_AUTH_SECRET=your_secret_here
